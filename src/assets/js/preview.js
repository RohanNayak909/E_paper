function openNewSection(img_id, id, i, cat, date,desc) {
    window.open("/preview/" + img_id + "/" + id + "/" + cat + "/" + date + "/" + i,'Prameya EPaper',desc)
    // window.open("/preview/" + img_id + "/" + id + "/" + cat + "/" + date + "/" + i,'_blank','toolbar=0,location=0,menubar=0')
}
